using ImageChartsLib;
using VacationAPI.Context;
using VacationAPI.Services.RequestManager;

namespace VacationAPI.Request.Teams.Get;

public class TeamStats
{
	// public static IResult GetStats(ApplicationContext db, string username, string accessToken, string teamName)
	// {
	// 	var request = Manager.CheckRequest(db, username, accessToken, teamName: teamName);
	// 	Entities.Team team = db.Teams.FirstOrDefault(x => x.Name == teamName && x.User.Name == username);
	//
	// 	if (request == null)
	// 	{
	// 		ImageCharts chart = new ImageCharts().c
	// 	}
	// 	return request;
	// }
}